package model;

import java.util.ArrayList;

// Represents a wishlist which contains ArrayList of Wish objects
public class WishList {
    private ArrayList<Wish> wishList;

    // EFFECTS: constructs a new wishlist
    public WishList() {
        wishList = new ArrayList<>();
    }

    public void addWish(String name, String brand, int price) {
        // stub
    }

    public void deleteWish(String name, String brand, int price) {
        // stub
    }

    public ArrayList<Wish> getWishList() {
        return wishList;
    }

    // student completes task example in lecture
    // got back list, verify tasks in list

    // user class that constructs wishlist and to buy list, list to keep track of friends
    // we can use java
    // make sure to have loops and branch, if, switch loop th

    // field with type WishList (clear WishList, get WishList) (test all methods in user class)

    // another class to keep track of users

    // user ID and password (two properties user would need to have), name,

    // another class to just store the user, this class has method called authenticate where we pass in user ID and password that was typed in the keyboard, it returns user 
    // create some fake users (populate the list)

    // add item from friend's list to our own list
}
